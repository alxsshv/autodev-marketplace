# План устранения несоответствия в документации по событиям Keycloak

**Дата создания плана:** 2026-06-14  
**Проблема:** Несогласованность в названиях webhook событий от Keycloak (регистр букв)

---

## Суть проблемы

В документации используется разное название webhook событий от Keycloak:
- В `auth-service-events.md`: `event: "user_enabled"` (строчные буквы) в JSON примерах, но `USER_ENABLED` в описании
- В `user-registration-architecture.md`: `event: 'user_enabled'` в цепочке, но `USER_ENABLED` в таблице
- В `communication-policy.md`: не указаны названия webhook событий от Keycloak, только Kafka события

**Keycloak на самом деле отправляет webhook события в формате snake_case с маленькими буквами:**
- `user_created`, `user_updated`, `user_enabled`, `user_disabled`

---

## Цель

Привести все названия webhook событий к единому стандарту, соответствующему реальному поведению Keycloak.

---

## Изменения

### 1. Файл: `docs/architecture/kafka-events/auth-service-events.md`

#### Изменения в секции "Обзор" (строка ~15):

**Было:**
```markdown
**Важно:** Все события синхронизации пользователей между Keycloak и PostgreSQL настроены через **Keycloak Events Provider** с типами: `USER_CREATED`, `USER_UPDATED`, `USER_ENABLED`, `USER_DISABLED`. Детальная настройка и описание процессов см. в `security/user-registration-architecture.md`.
```

**Стало:**
```markdown
**Важно:** Все события синхронизации пользователей между Keycloak и PostgreSQL настроены через **Keycloak Events Provider**. Keycloak отправляет webhook события со следующими названиями: `user_created`, `user_updated`, `user_enabled`, `user_disabled` (snake_case, строчные буквы). Детальная настройка и описание процессов см. в `security/user-registration-architecture.md`.
```

#### Изменения в блок-схеме архитектуры (строка ~26):

**Было:**
```markdown
│ Webhook (USER_CREATED, USER_UPDATED, USER_ENABLED, USER_DISABLED)
```

**Стало:**
```markdown
│ Webhook (user_created, user_updated, user_enabled, user_disabled)
```

#### Изменения в описании каждого события (4 события):

**Для auth.user.registered (строка ~61):**
- **Было:** `event: \`USER_CREATED\``  
- **Стало:** `event: \`user_created\``

**Для auth.user.updated (строка ~99):**
- **Было:** `event: \`USER_UPDATED\``  
- **Стало:** `event: \`user_updated\``

**Для auth.user.enabled (строка ~173):**
- **Было:** `event: \`USER_ENABLED\``  
- **Стало:** `event: \`user_enabled\``

**Для auth.user.disabled (строка ~211):**
- **Было:** `event: \`USER_DISABLED\``  
- **Стало:** `event: \`user_disabled\``

#### Изменения в разделе FAQ (строка ~567):

**Было:**
```markdown
| **Enabled Events** | `USER_CREATED`, `USER_UPDATED`, `USER_ENABLED`, `USER_DISABLED` | События, которые нужно отправлять |
```

**Стало:**
```markdown
| **Enabled Events** | `user_created`, `user_updated`, `user_enabled`, `user_disabled` | События, которые нужно отправлять (в формате snake_case) |
```

**Добавить примечание под таблицей:**
```markdown
**Важно:** Имена событий должны быть в формате snake_case с маленькими буквами, как указано в таблице. Это соответствует формату, который отправляет Keycloak Events Provider.
```

---

### 2. Файл: `docs/architecture/security/user-registration-architecture.md`

#### Изменения в таблице событий (строка ~115):

**Было:**
| Событие Keycloak | Описание | Действие Auth Service |
|------------------|----------|----------------------|
| `USER_CREATED` | Создание нового пользователя | Создать запись в `auth.users`, отправить `auth.user_registered` в Kafka |
| `USER_UPDATED` | Обновление данных пользователя | Обновить запись в `auth.users`, отправить `auth.user_updated` в Kafka |
| `USER_ENABLED` | Активация пользователя (enabled=true) | Обновить `enabled=true` в `auth.users`, отправить `auth.user.enabled` в Kafka |
| `USER_DISABLED` | Деактивация пользователя (enabled=false) | Обновить `enabled=false` в `auth.users`, отправить `auth.user.disabled` в Kafka |

**Стало:**
| Событие Keycloak | Описание | Действие Auth Service |
|------------------|----------|----------------------|
| `user_created` | Создание нового пользователя | Создать запись в `auth.users`, отправить `auth.user_registered` в Kafka |
| `user_updated` | Обновление данных пользователя | Обновить запись в `auth.users`, отправить `auth.user_updated` в Kafka |
| `user_enabled` | Активация пользователя (enabled=true) | Обновить `enabled=true` в `auth.users`, отправить `auth.user.enabled` в Kafka |
| `user_disabled` | Деактивация пользователя (enabled=false) | Обновить `enabled=false` в `auth.users`, отправить `auth.user.disabled` в Kafka |

#### Изменения в цепочке операций (строка ~126):

**Было:**
```markdown
Keycloak отправляет webhook в Auth Service: `POST /api/v1/auth/sync` (event: `user_enabled`)
```

**Стало:**
```markdown
Keycloak отправляет webhook в Auth Service: `POST /api/v1/auth/sync` (event: `user_enabled`)
```

*(Это уже правильно, не нужно менять)*

---

### 3. Файл: `docs/architecture/communication-policy.md`

#### Добавить новый раздел в конец документа (перед "Ссылки"):

```markdown
## Webhook события от Keycloak

Keycloak отправляет webhook события в Auth Service со следующими названиями:

| Название события | Описание |
|------------------|----------|
| `user_created` | Создание нового пользователя |
| `user_updated` | Обновление данных пользователя |
| `user_enabled` | Активация пользователя (изменение статуса enabled=true) |
| `user_disabled` | Деактивация пользователя (изменение статуса enabled=false) |
| `user_deleted` | Удаление пользователя |

**Важно:** Все названия webhook событий пишутся в формате snake_case с маленькими буквами.

Эти события настраиваются в Keycloak через **Events Provider** → **HTTP Events Provider** в разделе **Enabled Events**.

Kafka события (внутри системы) используют другой формат названий:
- `auth.user.registered` (соответствует `user_created`)
- `auth.user.updated` (соответствует `user_updated`)
- `auth.user.enabled` (соответствует `user_enabled`)
- `auth.user.disabled` (соответствует `user_disabled`)
- `auth.user.deleted` (соответствует `user_deleted`)
```

---

## Проверка

После внесения изменений:

1. Все webhook события от Keycloak должны использовать **snake_case с маленькими буквами**:
   - `user_created`, `user_updated`, `user_enabled`, `user_disabled`, `user_deleted`

2. Все Kafka события должны использовать **camelCase с префиксом**:
   - `auth.user.registered`, `auth.user.updated`, `auth.user.enabled`, `auth.user.disabled`, `auth.user.deleted`

3. В документации должно быть четкое разделение между webhook событиями (от Keycloak) и Kafka событиями (внутри системы)

4. Во всех примерах JSON должны быть корректные названия событий

---

## Критичность

**HIGH** - ключевая проблема при настройке Keycloak Events Provider. Разработчики могут настроить неправильные имена событий, что приведет к потере синхронизации.
